;(function (define) {
    'use strict';

    define(['backbone',
            'underscore',
            'gettext'],
           function (Backbone, _, gettext) {
               var view = Backbone.View.extend({
                   initialize: function() {
                      // TODO
                   },

                   render: function() {
                      // TODO
                   },
               });

               return view;
           });
}).call(this, define || RequireJS.define);
